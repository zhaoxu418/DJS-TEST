import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  contain: {
    backgroundColor: 'transparent',
  },
  contentView: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 10,
  },
  textView: {
    color: '#1e1e1e',
    fontSize: 22,
  },
});
